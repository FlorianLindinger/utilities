mention what settings where changed in vscode and where the file is. maybe prompt if it would change a setting that exists and is not correct alreafy

make it very low level python so it still runs. anyway only for winget i guess. mention somewhere the lowest possible.

gray out jupyter path if not enables